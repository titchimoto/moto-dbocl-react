import React from 'react';
import {DateRowContainer} from '../ui/containers';

const DateRow = ({date}) => (
  <DateRowContainer>
    <span>ICON</span>
    <span>{date}</span>
  </DateRowContainer>
)

export default DateRow;
